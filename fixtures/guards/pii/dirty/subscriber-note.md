# Synthetic dirty fixture (pii-guard unit tests)

This file intentionally contains a synthetic email address so tests can point
the guard at it with the allowlist disabled. It sits under fixtures/, which is
the allowlisted directory, so repo-wide guard runs stay green (design.md 1.1).

Contact: synthetic.reader@example.com
